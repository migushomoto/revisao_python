Faça uma função que percorra todas as strings de uma lista transforme em minusculo e remova
os caracteres acentuados:


```python
    def removeAcentos(array_entrada):
        return array_entrada
```